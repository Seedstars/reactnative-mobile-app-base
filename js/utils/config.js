import Config from 'react-native-config';

export const SERVER_URL = Config.BACKEND_URL;
// this is patch version that get's merged with APP_VERSION_NAME to create full version ie: 1.0.0
export const VERSION_PATCH = '0';

